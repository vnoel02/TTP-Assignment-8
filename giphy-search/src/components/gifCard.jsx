import SearchField from "./seachfield";

const GifCard = () => {
    return (
        <SearchField />
        // Area of the app where other gifs would be displayed, trending gifs mainly
        
    );

}

export default GifCard;